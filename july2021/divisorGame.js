/**
 * @param {number} n
 * @return {boolean}
 */
 var divisorGame = function(n) {
    return n % 2 === 0

    // if (n === 0) return true

    // const isValid = num => {
    //     return num < n && n % num === 0
    // }

    // if (isValid(n)) {
    //     return divisorGame(n - x)
    // }
};
console.log(
    divisorGame(2),//true

)